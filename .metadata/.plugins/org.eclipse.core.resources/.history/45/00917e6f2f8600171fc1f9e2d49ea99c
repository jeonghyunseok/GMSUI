package com.gms.web.proxy;

import java.util.Map;

import javax.servlet.http.HttpServletRequest;

import lombok.Setter;

public class PaginationProxy extends Proxy{
	@Setter
	
	Integer pageSize,blockSize;
	public PaginationProxy(HttpServletRequest request) {
		super(request);
		
	}
	private static BlockHandler ph=new  BlockHandler();
	private static PageHandler rh=new  PageHandler();
	
	@Override
	public void execute(Map<?, ?> map) {
		HttpServletRequest request = (HttpServletRequest)map.get("");
	
		
	}

}
